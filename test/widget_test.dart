import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App loads without errors', (WidgetTester tester) async {
    // Placeholder: app requires Firebase initialization, tested in integration tests
    expect(true, isTrue);
  });
}
